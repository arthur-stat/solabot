# JVM OOM 排查全过程记录（会话回顾）

- 记录日期：2026-02-16
- 记录范围：本次会话中，为定位线上 `java.lang.OutOfMemoryError: Java heap space` 所执行的排查命令、操作、步骤
- 约束：线上仅执行只读操作，不做任何写入/变更

## 1. 排查目标与方法

### 1.1 目标
- 复现并定位线上 OOM 的根因链路，而不是只看表层报错（Tomcat async timeout）

### 1.2 方法
- 本地源码只读排查：先找可能的内存增长点
- 远程运行态只读排查：JVM 参数、堆占用、对象直方图、GC 状态、进程状态、日志有效性
- 交叉验证：源码 -> 线上运行字节码 -> 运行态证据闭环

---

## 2. 本地排查步骤（命令 + 信息）

> 以下为本地命令与作用摘要（按时间顺序）

1. `Get-Location | Select-Object -ExpandProperty Path`
- 结果：确认当前目录为 `C:\Users\asheo\Desktop\workspace\bot`

2. `Get-ChildItem -Force | Select-Object Mode,Length,LastWriteTime,Name`
- 结果：确认项目结构（`src`、`pom.xml`、`README.md`、`target` 等）

3. `rg --files src pom.xml README.md`
- 结果：列出源码与配置文件清单

4. `rg -n "@Async|CompletableFuture|...|OutOfMemoryError" src pom.xml README.md`
- 结果：快速定位到高风险类：
  - `EchoWaiter`
  - `OneBotWsController`
  - `OneBotSender`
  - `OneBotSimpleActionBuilder`
  - 图片/GIF处理类等

5. `Get-Content src/main/java/com/arth/solabot/adapter/controller/websocket/OneBotWsController.java`
6. `Get-Content src/main/java/com/arth/solabot/adapter/sender/impl/OneBotSender.java`
7. `Get-Content src/main/java/com/arth/solabot/adapter/fetcher/EchoWaiter.java`
- 关键信息：
  - `EchoWaiter.complete()` 使用 `pending.computeIfAbsent(...)`
  - `await()` 才做 `pending.remove(echo)`

8. `Get-Content src/main/java/com/arth/solabot/adapter/sender/action/impl/OneBotSimpleActionBuilder.java`
9. `Get-Content src/main/java/com/arth/solabot/adapter/sender/action/SimpleActionBuilder.java`
- 关键信息：普通发送动作普遍自动带 `echo`（`echo-<timestamp>`）

10.（一次失败命令）
- `rg -n "echo\"|\"echo|build.*Action\(" ...`
- 结果：PowerShell 转义导致命令失败，不影响后续

11. `rg -n "CompletableFuture|newFixedThreadPool|ExecutorService|ThreadPoolExecutor|..." src/main/java/com/arth/solabot`
12. `Get-Content src/main/java/com/arth/solabot/core/infrastructure/config/ConcurrencyConfig.java`
13. `Get-Content src/main/java/com/arth/solabot/plugin/custom/Gallery.java`
- 关键信息：命令线程池是有界队列 `ArrayBlockingQueue(1000)` + `AbortPolicy`，不符合“无界队列撑爆”模式

14. `Get-Content src/main/resources/application.yaml`
- 关键信息：`app.cmd-thread-timeout: 600`；默认日志级别 `INFO`

15. `Get-FileHash`（3个关键文件）
- 本地文件：
  - `EchoWaiter.java`
  - `OneBotWsController.java`
  - `OneBotSimpleActionBuilder.java`
- 用于和远程同路径文件做哈希对照

16. `rg -n "\.request\(" src/main/java/com/arth/solabot | Select-Object -First 200`
17. `rg -n "echoWaiter\.register|register\(" src/main/java/com/arth/solabot/adapter | Select-Object -First 200`
18. 查看 `OneBotSender` 行号片段（`request` 路径）
- 关键信息：`register+await` 只发生在 `sender.request(...)` 场景

---

## 3. 远程排查步骤（所有与 OOM 排查相关命令）

> 重点：以下为本次排查 JVM OOM 原因所执行的远程命令（含失败命令）

### 3.1 SSH 可用性与连接

R01. `ssh -o StrictHostKeyChecking=no root@182.92.168.150 "hostname; date; ps -ef | grep -i java | grep -v grep"`
- 结果：失败（命中了本机 `ssh.bat` 包装）
- 信息：需要改用系统 `ssh.exe`

R02. `C:\Windows\System32\OpenSSH\ssh.exe -o StrictHostKeyChecking=no root@182.92.168.150 "hostname; date; ps -ef | grep -i java | grep -v grep"`
- 结果：成功
- 输出关键信息：
  - 主机：`iZ2ze2if7lrdhpqwos5mftZ`
  - 时间：`Mon Feb 16 03:18:36 PM CST 2026`
  - Java进程：
    - Maven `spring-boot:run` 进程
    - 业务 JVM PID `11495`

### 3.2 线上工程与 JVM 参数/堆状态

R03. `ssh.exe root@182.92.168.150 "cd /root/ichika/solabot && pwd && ls -la"`
- 结果：确认线上项目路径和文件（含 `full.log`）

R04. `ssh.exe root@182.92.168.150 "jcmd 11495 VM.flags; echo '---'; jcmd 11495 GC.heap_info"`
- 结果：成功
- 关键信息：
  - `MaxHeapSize=423624704`（约 404MB）
  - `UseSerialGC`
  - 新生代、老年代几乎 100% 满

R05. `ssh.exe root@182.92.168.150 "cd /root/ichika/solabot && find . -maxdepth 3 -type f \( -name '*.log' -o -name 'nohup.out' \) | sed 's#^./##'"`
- 结果：只发现 `full.log`

### 3.3 对象分布（证据核心）

R06. `ssh.exe root@182.92.168.150 "jcmd 11495 GC.class_histogram | head -n 120"`
- 结果：成功
- 关键信息（Top）：
  - `[B`：约 `375,985,848` bytes
  - `java.lang.String`：约 `314k`
  - `ConcurrentHashMap$Node`：约 `138k`
  - `CompletableFuture`：约 `95k`

R07. `ssh.exe root@182.92.168.150 "jcmd 11495 GC.class_histogram | egrep 'CompletableFuture|ConcurrentHashMap|java.lang.String|OneBot|EchoWaiter|JsonNode|LinkedHashMap|HashMap\$Node|char\[|byte\[' | head -n 120"`
- 结果：成功
- 信息：`CompletableFuture + ConcurrentHashMap$Node + String` 数量异常，吻合待清理关联表堆积

### 3.4 日志检查（验证日志是否有效）

R08. `ssh.exe root@182.92.168.150 "cd /root/ichika/solabot && rg -n 'OutOfMemoryError|Error processing async timeouts|...' full.log | tail -n 200"`
- 结果：失败（远程无 `rg`）

R09. `ssh.exe root@182.92.168.150 "cd /root/ichika/solabot && rg -n 'OutOfMemoryError|Error processing async timeouts' full.log"`
- 结果：失败（远程无 `rg`）

R10. `ssh.exe root@182.92.168.150 "cd /root/ichika/solabot && tail -n 200 full.log"`
- 结果：成功
- 信息：`full.log` 是 2025-12-01 启动失败旧日志，不是当前运行日志

### 3.5 配置对照（线上 application.yaml）

R11. `ssh.exe root@182.92.168.150 "cd /root/ichika/solabot && sed -n '1,220p' src/main/resources/application.yaml"`
- 结果：成功
- 信息：线上配置和本地关键项一致（如 `cmd-thread-timeout: 600`）

R12. `ssh.exe root@182.92.168.150 "cd /root/ichika/solabot && ls -la src/main/resources && ls -la src/main/resources/logback* 2>/dev/null || true"`
- 结果：成功
- 信息：未见额外 logback 文件

### 3.6 进程内存与系统压力

R13. `ssh.exe root@182.92.168.150 "cat /proc/11495/status | egrep 'Name|VmRSS|VmSize|Threads|FDSize|VmSwap|RssAnon|RssFile|RssShmem'"`
- 结果：成功
- 关键信息：
  - `VmRSS ~526MB`
  - `VmSwap ~202MB`
  - `Threads 44`

R14. `ssh.exe root@182.92.168.150 "ps -p 11495 -o pid,etime,%cpu,%mem,rss,vsz,cmd --cols 220"`
- 结果：成功
- 信息：进程运行很久，内存占用高

R15. `ssh.exe root@182.92.168.150 "jcmd 11495 VM.native_memory summary 2>/dev/null | head -n 80"`
- 结果：成功（提示 NMT 未开启）
- 信息：无法用 NMT 进一步拆 native 内存

R16. `ssh.exe root@182.92.168.150 "jstat -gcutil 11495 1000 5"`
- 结果：成功
- 关键信息：
  - `E=100`, `O=100`
  - `FGC` 连续增长（短时内增加多次）
  - 典型 Full GC 风暴

R17. `ssh.exe root@182.92.168.150 "free -m"`
R18. `ssh.exe root@182.92.168.150 "grep -E 'MemTotal|MemFree|SwapTotal|SwapFree' /proc/meminfo"`
R19. `ssh.exe root@182.92.168.150 "cat /sys/fs/cgroup/memory.max 2>/dev/null || cat /sys/fs/cgroup/memory/memory.limit_in_bytes 2>/dev/null || echo no-cgroup-limit-file"`
- 结果：成功
- 信息：无明显 cgroup 限制文件；主机内存总量约 1.6GB，swap 存在

R20. `ssh.exe root@182.92.168.150 "jcmd 11495 Thread.print | egrep -c 'cmd-|http-nio|nioEventLoop|ForkJoinPool|alina-utility'"`
- 结果：成功（计数 48）
- 信息：线程数量不显示失控级别增长

### 3.7 运行文件活跃性、环境变量

R21. `ssh.exe root@182.92.168.150 "find /root/ichika/solabot -type f -mmin -180 2>/dev/null | head -n 200"`
- 结果：空
- 信息：进一步印证当前运行日志可能不在仓库文件中

R22. `ssh.exe root@182.92.168.150 "tr '\0' '\n' < /proc/11495/environ | egrep 'JAVA|JDK|MAVEN|SPRING|_OPTS'"`
- 结果：成功
- 信息：仅见 Maven 参数，无额外 `JAVA_OPTS` 暴露

### 3.8 线上/本地源码一致性 + 运行字节码验证

R23. `ssh.exe root@182.92.168.150 "sha256sum /root/ichika/solabot/src/main/java/com/arth/solabot/adapter/fetcher/EchoWaiter.java /root/ichika/solabot/src/main/java/com/arth/solabot/adapter/controller/websocket/OneBotWsController.java /root/ichika/solabot/src/main/java/com/arth/solabot/adapter/sender/action/impl/OneBotSimpleActionBuilder.java"`
- 结果：成功
- 信息：哈希与本地不一致（主要可能是换行风格），需看内容与字节码

R24. `ssh.exe root@182.92.168.150 "nl -ba /root/ichika/solabot/src/main/java/com/arth/solabot/adapter/fetcher/EchoWaiter.java"`
R25. `ssh.exe root@182.92.168.150 "nl -ba /root/ichika/solabot/src/main/java/com/arth/solabot/adapter/controller/websocket/OneBotWsController.java"`
R26. `ssh.exe root@182.92.168.150 "nl -ba /root/ichika/solabot/src/main/java/com/arth/solabot/adapter/sender/action/impl/OneBotSimpleActionBuilder.java"`
- 结果：成功
- 信息：线上源码逻辑与本地关键逻辑一致

R27. `ssh.exe root@182.92.168.150 "javap -classpath /root/ichika/solabot/target/classes -c com.arth.solabot.adapter.fetcher.EchoWaiter"`
- 结果：成功
- 关键信息：运行字节码确认 `complete()` 和 `await()` 都是 `computeIfAbsent` 路径（排查时线上版本）

R28. `ssh.exe root@182.92.168.150 "javap -classpath /root/ichika/solabot/target/classes -c com.arth.solabot.adapter.sender.action.impl.OneBotSimpleActionBuilder | egrep 'echo-|setEcho|send_msg'"`
- 结果：失败（过滤命令未匹配输出）

R29. `ssh.exe root@182.92.168.150 "javap -classpath /root/ichika/solabot/target/classes -verbose com.arth.solabot.adapter.sender.action.impl.OneBotSimpleActionBuilder | grep -n 'echo-' | head"`
- 结果：成功
- 信息：常量池可见 `echo-` 模板

R30. `ssh.exe root@182.92.168.150 "javap -classpath /root/ichika/solabot/target/classes -c com.arth.solabot.adapter.controller.websocket.OneBotWsController | sed -n '120,220p'"`
R31. `ssh.exe root@182.92.168.150 "javap -classpath /root/ichika/solabot/target/classes -c com.arth.solabot.adapter.controller.websocket.OneBotWsController | sed -n '70,140p'"`
- 结果：成功
- 信息：运行字节码确认回包分流确实调用 `echoWaiter.complete(...)`

R32. `ssh.exe root@182.92.168.150 "jcmd 11495 GC.class_histogram | awk '/\[B \(java.base/{print;getline;print;getline;print;getline;print}'"`
- 结果：成功
- 信息：再次确认 `[B`、`String`、`ConcurrentHashMap$Node`、`CompletableFuture`位居前列

R33. `ssh.exe root@182.92.168.150 "ps -p 11495 -o lstart="`
- 结果：成功
- 信息：启动时间 `Tue Dec 2 10:39:31 2025`（长时间运行）

---

## 4. 排查过程中形成的证据链（从命令结果到结论）

1. 堆上限偏小：`MaxHeapSize ~404MB`（R04）
2. 现场已堆满：新生代/老年代都接近100%（R04、R16）
3. GC风暴：`FGC` 持续增长（R16）
4. 对象特征：`CompletableFuture` + `ConcurrentHashMap$Node` + `String/[B` 异常堆积（R06/R07/R32）
5. 协议行为：普通发送动作普遍携带 `echo`（本地源码步骤 5~9）
6. 分流行为：WS 收到无 `post_type` 且有 `echo` 的回包，走 `echoWaiter.complete(...)`（步骤 5 + R31）
7. 清理缺口：`EchoWaiter.complete()` 会对未知 `echo` `computeIfAbsent` 创建新 future；仅 `await()` 才删除（步骤 7 + R27）
8. 实际调用范围：`register+await` 只在 `request()` 场景（本地步骤 16~18）

=> 结论：普通发送产生的大量回包 `echo` 会把 `EchoWaiter.pending` 变成只增不减，长期运行后触发 OOM。

---

## 5. 当时给出的排查结论

- 你给出的 Tomcat 日志（`Error processing async timeouts` + `ExecutionException: OutOfMemoryError`）是“堆耗尽后的触发点”，不是根因。
- 根因链路是：`echo` 关联缓存泄漏（`EchoWaiter.pending`）+ 进程长期运行 + 小堆上限。

---

## 6. 线上写操作核对

- 为排查 OOM 过程，线上执行命令均为只读（`ps/jcmd/jstat/cat/grep/sed/javap/sha256sum/find`）。
- 未执行任何 `echo > file`、`tee` 写入、`git commit`、`git checkout`、`rm`、`mv`、部署/重启类命令。

---

## 7. 备注

- `full.log` 为历史日志（R10），不是当前运行日志；最终定位依赖 JVM 运行态证据与源码/字节码交叉验证。
- 该文档仅记录“排查阶段”动作与结论，不包含后续本地修复提交细节。
